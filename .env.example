# Copy this file to .env and fill in your values.
# .env is never committed — see .gitignore.

# --- Provider selection ---

# Which AI provider to use: gemini (macOS default) or azure (Windows)
# PROVIDER=gemini

# --- OBS (required for all providers) ---

# OBS WebSocket password (Tools → WebSocket Server Settings in OBS)
OBS_PASSWORD=

# OBS WebSocket host/port (default: localhost:4455)
# OBS_HOST=localhost
# OBS_PORT=4455

# --- Gemini (required when PROVIDER=gemini) ---

# Gemini API key (https://aistudio.google.com/apikey)
GEMINI_API_KEY=

# --- Azure (required when PROVIDER=azure) ---

# Azure AI Speech - for transcription + speaker diarization
# Create a Speech resource in Azure Portal (Cognitive Services → Speech)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# Azure OpenAI - for summarization
# Create an Azure OpenAI resource and deploy a chat-completions model (e.g. gpt-5-mini)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini

# --- Optional ---

# Directories for recordings and transcripts (default: ./recordings, ./transcripts)
# RECORDINGS_DIR=./recordings
# TRANSCRIPTS_DIR=./transcripts

# Web server port (default: 8080)
# PORT=8080

